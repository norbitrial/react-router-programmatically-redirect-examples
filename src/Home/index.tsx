import React from 'react';
import './styles.css';

const Home = () => {
    return <>
        <p className="title">Select actions from above to redirect programmatically to different routes.</p>
        <p className="description">By clicking each buttons the rendered route will represent what code solution has been used to redirect.</p>
        Let's try <span role="img" aria-label="flame">🔥</span>
    </>
}

export default Home;