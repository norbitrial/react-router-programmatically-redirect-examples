import React from 'react';

const UseHistoryResult = () => {
    return <>
        <p>Redirected by using <code>useHistory</code> hook.</p>
        <p>Take a look at the component called <code>UseHistoryAction</code> how to implement.</p>
        <p>The <code>useHistory</code> gives us access to the history object which helps us programmatically navigate or change routes.</p>
    </>
}

export default UseHistoryResult;